# Scope publish entitlements to one invitation

Each `Vowly Publish — 1 Year` purchase creates or renews the hosting entitlement for exactly one Invitation. The entitlement is not account-wide, transferable, or pooled in the MVP, which keeps publication limits, renewal, refunds, and support behavior unambiguous while the product model is still being validated.
