# Retain media referenced by published versions

Removing media from a Draft invitation detaches it from future drafts but does not delete it while any Published invitation references it. This prevents an Owner's normal editing workflow from breaking the currently public invitation; storage cleanup waits until no published version or recovery period needs the media.
