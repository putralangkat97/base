# Archive events instead of deleting RSVP history

An Event with existing RSVP responses is archived rather than deleted. Archiving prevents new responses while preserving the Event and its reporting/export history, so changing the invitation schedule cannot silently destroy attendance records.
