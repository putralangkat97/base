# Keep the published version immutable

Vowly keeps the Owner's Draft invitation separate from the Published invitation shown to visitors. Editing changes only the draft, and publication creates a new public version after validation; a failed or incomplete publish never changes the currently public version. This prevents half-finished edits from leaking to guests and makes republishing explicit and recoverable.
